{
  "name": "infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy --outputs-file ../cdk-outputs.json",
    "destroy": "cdk destroy",
    "test": "tsx --test lib/**/*.test.ts"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.177.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "aws-cdk": "^2.177.0",
    "esbuild": "^0.24.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
