export function ChatPage() {
  return (
    <section className="narrow">
      <h1>Assistant</h1>
      <p className="lede">
        The in-app shopping assistant is Phase 5. This route is signed-in only so
        cart and orders stay behind the same login wall.
      </p>
    </section>
  );
}
